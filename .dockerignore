# If we don't specify any files to ignore, Docker will send everything from this directory down to the Docker Daemon.

.github/
.vscode/
coverage/
node_modules/

# Exclude tests but allow .htpasswd file
tests/
!tests/.htpasswd

eslint.config.mjs
.git/
.gitignore
.prettier*
jest*

